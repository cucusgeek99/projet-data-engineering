package com.ecommerce.analytics

// TODO (Membre C) - Q6.1 : object principal orchestrant le pipeline
// (ingestion -> validation -> transformation -> analytique -> écriture CSV/Parquet),
// SparkSession externalisée, gestion d'erreurs globale + arrêt propre.
